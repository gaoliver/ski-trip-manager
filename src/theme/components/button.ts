import { ComponentStyleConfig } from "@chakra-ui/react";

export const Button: ComponentStyleConfig = {
  baseStyle: {
    _hover: {
      cursor: "pointer",
    },
  },
  defaultProps: {},
  variants: {},
};
